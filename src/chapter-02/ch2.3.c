#include <stdio.h>

int main(void) {
  int num_aluno;
  printf("Introduz o teu número de aluno: ");
  scanf("%d", &num_aluno);
  printf("Bem-vindo a aula de programacao, aluno %d! Vamos divertir-nos imenso!\n", num_aluno);
  return 0;
}
